const SaveButton = () => {
  return (
    <button
      id="saveTask"
      className="px-5 py-2 bg-green-400 text-white font-semibold rounded-lg shadow-md hover:bg-green-400 hover:ring-2 hover:ring-1 hover:ring-green-400 focus:outline-none focus:ring-2 focus:ring-green-200 transition"
    >
      Save
    </button>
  );
};

export default SaveButton;
